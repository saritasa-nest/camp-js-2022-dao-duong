/** Type of token. */
export enum Token {
  Refresh = 'REFRESH_TOKEN',
  Access = 'ACCESS_TOKEN',
}

/** Available url. */
export enum Url {
  Home = '/',
  Login = '/login/',
  Register = '/register/',
  Profile = '/profile/',
}
