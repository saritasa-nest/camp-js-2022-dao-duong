/** Sort setting. */
export interface SortSetting {

  /** Option for sort. */
  option: string;

  /** Direction for sort. */
  direction: string;
}

/** Sort options. */
export interface SortOption {

  /** Sort option text. */
  text: string;

  /** Sort option value. */
  value: string;
}
